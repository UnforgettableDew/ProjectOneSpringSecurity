server:
  port: 8080
spring:
  application:
    name: security_test
  datasource:
    username: 'unforgettable'
    password: '27092100'
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/security_test?useUnicode=true&serverTimezone=UTC
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true

